<%- include('partials/head') %>

<div class="min-h-screen flex flex-col items-center justify-center p-6 text-center">
  <div class="text-7xl font-extrabold bg-gradient-to-r from-blue-400 to-cyan-300 bg-clip-text text-transparent mb-4"><%= code %></div>
  <h1 class="text-2xl font-bold mb-2"><%= title %></h1>
  <p class="text-white/50 mb-8"><%= message %></p>
  <a href="/" class="px-8 py-3 rounded-full bg-gradient-to-r from-blue-600 to-blue-500 font-semibold">Volver al inicio</a>
</div>

<%- include('partials/footer') %>
